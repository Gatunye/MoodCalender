import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const styles = StyleSheet.create({
    root: {
        width: "100%",
        height: "100%",
        backgroundColor: ""
    }
})

export default function Calender(props){
    return <View styles={syles.root}>
<Text>
    Screen one
</Text>
    </View>
}